<?xml version="1.0" encoding="UTF-8"?>
<Configuration>

	<Appenders>

		<RollingFile name="File" fileName="/config/logs/davos.log" filePattern="/config/logs/${date:yyyy-MM}/app-%d{yyyy-MM-dd-HH}-%i.log">
			<PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} - %5p - [%c{1}] - %msg%n" />
			<Policies>
				<SizeBasedTriggeringPolicy size="10 MB" />
			</Policies>
		</RollingFile>

		<!--
			Routes schedule run logs to a dedicated file per schedule. The
			RunnableSchedule sets the "scheduleName" thread context value while a
			run is in progress; events without it (web requests, startup) fall
			back to the main davos.log file.
		-->
		<Routing name="ScheduleRouting">
			<Routes pattern="$${ctx:scheduleName:-_main}">

				<Route key="_main" ref="File" />

				<Route>
					<RollingFile name="Schedule-${ctx:scheduleName}"
						fileName="/config/logs/schedules/${ctx:scheduleName}.log"
						filePattern="/config/logs/schedules/${ctx:scheduleName}-%d{yyyy-MM-dd}-%i.log">
						<PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} - %5p - [%c{1}] - %msg%n" />
						<Policies>
							<SizeBasedTriggeringPolicy size="10 MB" />
						</Policies>
					</RollingFile>
				</Route>

			</Routes>
		</Routing>

	</Appenders>

	<Loggers>

		<Logger name="io.linuxserver" level="info" additivity="false">
			<AppenderRef ref="ScheduleRouting" />
		</Logger>

		<Logger name="org.thymeleaf" level="warn" additivity="false">
			<AppenderRef ref="File" />
		</Logger>

		<Logger name="org.springframework" level="error" additivity="false">
			<AppenderRef ref="File" />
		</Logger>

		<Root level="warn">
			<AppenderRef ref="File" />
		</Root>

	</Loggers>

</Configuration>
